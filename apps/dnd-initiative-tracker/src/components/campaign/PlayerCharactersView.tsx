import { CampaignManagerView } from './CampaignManagerView'

/**
 * Dedicated page for managing player character imports, drafts, and saved heroes.
 */
export const PlayerCharactersView: React.FC = () => {
  return <CampaignManagerView title="Player Characters" />
}
